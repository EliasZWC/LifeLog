# 更新日志

本项目的所有重要变更都会记录在此文件中。
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)，Git 标签格式为 `vX.Y.Z`。

## [v0.1.21] - 2026-09-24

### 变更

- **底部导航栏的图标改成「未选中线型 / 选中填充」两套**。四个图标各配一对
  `materialiconsoutlined` + `materialicons` 的官方图形，选中时换成实心，
  状态比原来只靠颜色深浅更好辨认。
  ⚠️ 「时间」原来的 `hourglass_full` 图形在两套里长得**完全一样**，换了也不看得出，
  所以改成 `schedule`（带指针的钟面），这个才有真正的线型/填充差别。

- **设置页底部的预留空间变大**（滚到底时最后一行不再贴住导航栏）。
  以前可滚动页面自身没有下内边距，只靠分组那 4px，实测滚到底间隙是 **0px**；
  现在统一在 `.page` 上留 24px，时间页 / 设置页都受益。

## [v0.1.20] - 2026-09-24

### 变更

- **时间页的「周」改成真实的自然周**。以前按「1–7 号算第 1 周」切，一个月被
  平均分成四大块，跟日历上的周完全对不上。现在按 **周一到周日** 切，
  周标题直接写起止日期（`09/21 ~ 09/27`）。
  代价是月初 / 月末那一周会横跨两个月，归属取该周**周一所在的月**，
  所以会出现「某月下只有半个月记录」这种正常情况。
  跨月、跨年的那一周现在也稳定地算作同一周（以前会从中间断开）。

- **设置页新增「时间」区，两项设置**：
  - **时区**：默认跟随系统，也可以自己挑一个固定偏移（`UTC-08:00` 这类）。
    改完时间页的分组、日期标记、记录表单里的时间都会跟着换算。
  - **每周起始日**：周日（默认）或周一。影响的是**一周从哪天开始展示**，
    归组口径本身固定按周一算，所以来回切换不会把记录换到别的周去。
    选择会记住，下次打开还是上次的值。

- **统计图点某一天的气泡，第一行显示是哪一天**。以前只列数值，
  横轴刻度又只标首/中/尾三处，点完常常不知道看的是哪天。
  现在第一行是 `09/22 周二`（带下划线与数值行分开），下面才是各项目的值 / 时长。

## [v0.1.19] - 2026-09-23

### 变更

- **跟踪数据也能导出 / 导入了**。以前只有时间记录能走 CSV，跟踪数据只存在
  `metrics.csv` 里、用户摸不到；现在点「导出数据 / 导入数据」会先让你选要处理
  哪一份（**时间记录** 或 **跟踪数据**），再走原来的系统「另存为」/ 文件选择器。
  导出文件名相应区分：`livolog-001.csv`（时间记录）、`livolog-metrics-001.csv`
  （跟踪数据）。跟踪数据的 CSV 是宽表，一列一个项目，可以直接在 Excel 里改完再导回来。
- **确认绿 / 删除红加深了一点**。日间 `#15803D` / `#C6211A`，夜间 `#7EE2A8` / `#FF8A80`。
  原来的取值偏灰，在浅色背景上不够醒目。

### 修复

- **导入 CSV 变成覆盖式操作以后，选错文件会把数据整个清空还提示成功**。
  `hello`、`world` 这种随便一个文本文件（或别的表格）被选中时，解析器一行都认不出来、
  却照样执行了「用文件内容替换全部数据」，于是既清了库又弹了「导入成功」。
  现在两种情况都会被拦住并提示具体原因，**原数据一个字节都不动**：
  - 不是 Livolog 的 CSV（缺少 `metric` / `time` 列，或时间记录缺 `behavior` / `start` 列）；
  - 格式对但没有一行能读出来（只有表头、或所有行的关键字段都解析失败）。
  提示也改成看得懂的话（「不是 Livolog 的 CSV（缺少 metric / time 列）」而不是错误码）。
  时间记录那边有同样的问题，一并修了。

## [v0.1.18] - 2026-09-22

### 变更

- **统计视图选的项目会记住了**。以前每次进详情页都重置成 `All`，切出去再回来
  选择就没了；现在按跟踪项存进本地，下次进来还是上次那几个。时间区间仍然每次
  重置（只有项目做记忆）。
- **时间范围那一行去掉了「Range / 时间范围」标签，改成开始贴左、结束贴右**。
  这一行本身就是个日期区间，说明文字是多余的；两端各占一边正好体现「从…到…」。
  两端的下拉箭头朝外，两个日期都紧贴自己那一侧。整行仍可分别点开改日期。
- **日期选择表单里的年月日输入改为居中**（统计视图的起止日期用它）。
  这个表单没有左侧标签，以前输入框贴左、右边空一大块；现在整组居中。
  ⚠️ 只改这个表单 —— 时间记录表单有「发生 / 开始 / 结束」标签，仍是标签左对齐、
  值贴右，不受影响。
- **时间范围的默认值改成「有记录的最早 → 最晚」**。以前是「最近 30 天」，
  早期的记录默认看不到；现在默认把全部记录框进来。

### 修复

- **在跟踪项里改了项目名字后，原来的记录还在、但值全部变成空**。原因是编辑表单
  只把项目**名字**交给后端，后端按名字匹配旧字段 —— 改了名就匹配不上，于是把
  它当成一个全新字段（分配新 id），老记录里存的旧 fieldId 就此失联。
  现在：
  - 编辑表单每个项目行带上原字段 id；
  - `updateMetric` 认字段的顺序改为 **按 id → 按名字 → 按位置**，
    所以改名、换顺序都不丢数据，只有真的是新增行才会拿到新 id。
  - 另外记忆的项目 id 如果已经被删掉，会自动回落到「全部」，不会画出空图。

## [v0.1.17] - 2026-09-22

### 变更

- **多项目折线的样式组合改成「形状外层循环 × 虚实内层循环」**。现在顺序是
  圆点实线 → 圆点虚线 → 方点实线 → 方点虚线 → 三角实线 → 三角虚线 → …
  这样**相邻两条线的形状一定不同**，比之前「先排完虚实再换形状」好认得多。
  虚线仍只有一种（花式虚线肉眼分不清），区分主要靠形状。
- **启动页放大**：logo 从 1.5 倍放大到 **2 倍**，下方 `LIVOLOG` 文字
  由 15px 跟到 **30px**，比例保持一致。

### 修复

- **只有两条记录时统计图画不出折线**。统计图以前是**按天聚合**的
  （一天一个点、同一天多次记录只取最后一个值），于是同一天的两条记录
  压缩成一个点，两条记录根本连不成线段。现在改成**一条记录一个点**，
  两点就能连成线段（同一天记三次就有三个点）。
  - 横轴刻度仍是日期：同一天的多个点共用一个日期标签，并且首/中/尾
    三处刻度会按日期去重，不会出现两个一样的 `9-22`。
  - 只有一条记录时（一个点连不成线）仍走原来的兜底：画一条横贯绘图区的短线 +
    放大标记。
  - 图表标题「每日取值」相应改为「每次记录取值」。

## [v0.1.16] - 2026-09-22

### 变更

- **统计图的点标记放大**（半径 2.6 → 4.2 viewBox 单位，真机约 10 物理像素）。
  之前虽然形状有区分，但点太小根本看不出是圆、方还是三角。

### 修复

- **跟踪项挂着一大堆不属于自己的项目**（如 Systolic / Diastolic / Pulse /
  weight / waistline / weight_2 / waistline_2 全挤在一个跟踪项里，值都是空）。
  两个根因：
  1. `applyStoredCsv` 把 CSV 里的**所有**值列都当成**每个**跟踪项的字段 ——
     但宽表里 A 的列在 B 的行里本来就是空的。现在按「本跟踪项自己的行里
     至少有一个非空值」来决定它有哪些列，不再无脑照单全收。
  2. `stringify` 给重名字段生成 `xxx_2` 列名，读回来时又被当成一个真名叫
     `xxx_2` 的新字段，每存读一轮就多一批。现在重名后缀改用 `~2`
     （不会出现在正常字段名里的分隔符），读回时剥掉，往返稳定。
  - 另加 `pruneUnusedFields()` 兜底修复：按「这个跟踪项的记录到底用到过哪些
    字段」剪掉多余的，**只删从来没有任何值的字段**，所以不会丢数据；
    每次读完 `metrics.csv` 自动跑一次，继承下来的脏数据会自动清干净。

## [v0.1.15] - 2026-09-22

### 变更

- **跟踪统计的「项目」选项改成 All / Select… 两步**。默认 `All`；点 `Select…`
  弹出居中的勾选清单，勾好按确定才生效。清单顶部有「全部」一行（勾满 = 回到 `All`，
  部分勾选时显示为半选状态），并且**至少会保留一个项目**（全取消会自动恢复），
  免得筛成空图让人以为坏了。全部选中后这一行显示回 `All`。
- **统计选项栏右侧的值后面加了下拉箭头**（▾）。之前右对齐的时间 / 值光看外观
  看不出能点，现在一眼就是下拉。
- **新建跟踪项表单的「添加项目」按钮改为右对齐**（原来贴左）。
- **绿色只留给确认按钮**。更新弹窗的「更新」是动作不是确认，改回普通前景色
  （新增 `.btn-action`）；`.btn-primary` 仍然只在 `× 确定 / 确认` 这类地方用绿色。
- **多项目折线只用「实线 + 一种虚线」，其余靠点标记形状区分**。原来堆了
  `5 3` / `1 3` / `7 3 2 3` / `2 2` / `9 3 2 3 2 3` 五种花式虚线，肉眼根本分不清
  哪条是哪条。现在固定 5 组样式：实线+圆点、虚线+方块、实线+三角、虚线+菱形、
  实线+叉，图例样例线也带同形状标记。

### 修复

- **只有一两天记录的跟踪项，统计图看起来是空的**。两条记录落在同一天时，
  按天聚合后只剩一个点，而折线至少要两个点才画得出来 —— 于是图上只有一个
  2.6px 的小圆点，看起来就是「空的」。现在单点也会画一条横贯绘图区的短线 +
  放大标记，能清楚看到这天的值。
- **某个跟踪项的记录里混进了别的跟踪项的项目**（值为空）。根因是 v0.1.13 给
  老数据补字段时用了**全局常量** `legacy-value` 当字段 id，于是所有老跟踪项的
  第一字段 id 都相同，而 `valueOf` 只按 fieldId 匹配、不看记录属于哪个跟踪项，
  A 的值就被 B 读到了。现在字段 id 改成**按跟踪项派生**（`legacy:<跟踪项id>`），
  并且 `valueOf` 增加「记录归属」校验；迁移里也加了把旧 `legacy-value`
  换算成新 id 的一步，同时不再把认不上的值硬塞进第一个字段。

## [v0.1.14] - 2026-09-22

### 变更

- **「常用」再精简到 12 个**，移除：
  - `monitor_weight`（体重秤，医疗健康）—— 就是那个方形轮廓 + 中间一个圆口
    + 三个点的图标，确实不好认；
  - `computer`（计算机，工作）。
  - 两个图标本身都还在各自分类里，只是不再出现在「常用」。

## [v0.1.13] - 2026-09-22

### 新增

- **图标扩到 90 个**，并新增 5 个图标（都是谷歌官方图标）：
  - 医疗健康：`cardiology`（心电图）、`genetics`（基因双螺旋）、
    `pill`（药丸）、`syringe`（注射器）
  - 学习：`book_4`
  - ⚠️ 这 5 个来自新的 **Material Symbols** 仓库（旧 `material-design-icons`
    的 `src/` 分类里还没有），坐标系是 `0 -960 960 960`，
    由 `tools/fetch-symbols.py` 统一换算成 24 网格后再写进 `icons.js`。

### 变更

- **图标分类重新调整**（9 类）：
  - 「学习工作」拆成 **学习** 和 **工作** 两类；
  - 「生活日常」改名为 **日常**；
  - `book` / `book_4` 归入**学习**；
  - 电话 `call`、邮件 `email`、星标 `star` 从「其他」移入**工作**；
  - `pill` 加入**常用**，`medication` 从常用移除。
- **「常用」精简到 14 个**，移除：自行车、手机、行走、月亮、电影、音乐、
  房子、打扫、铅笔、水滴、`medication`。
- **表单里填时间的值改为右对齐**（左对齐的提示文字「开始 / 结束」保持不变）；
  时点没有起止之分，补了提示文字 **Happen / 发生**，跟「开始 / 结束」一样对齐。
- **统计视图的两个选项对调了顺序**（跟踪项统计）：
  - **第一行**改成「**项目**」——选这次要看哪几个项目。默认「全部」，
    也可以只勾其中一个或两个（多选，再点一下取消；全部取消就回到「全部」）。
    线型按项目在跟踪项里的**固定位置**决定，所以增删勾选时同一条线的样式不会跳。
  - **第二行**放回「**时间范围**」（起止日期）。
  - 线型不再单独占一行当图例，**图例挪到图里面**（画在图表块内、折线图下方），
    每条线一个「线型样例 + 项目名」。只有一个项目时不给图例。
- **所有表单的确认按钮改成绿色**。新增 `--success` 主题变量，与「删除」的红色
  （`--danger`）同色系、同明度 / 降饱和水平（明色 `#1F6B3A` / 暗色 `#A9E5BC`），
  确认（`.btn-primary`）一律用它；不可用时仍退成灰色。影响范围：时间记录、
  行为、跟踪项、跟踪记录表单以及日期 / 更新弹窗的确认按钮。

### 修复

- **升级到 v0.1.12 后旧跟踪记录打不开 / 显示不出值**。两个独立原因叠加：
  1. 旧数据没有 `fields` 字段时，读取时每次都用 `newId()` 现造一个字段 id，
     于是**每读一次 id 就变一次**，老记录里存的 `fieldId` 永远对不上 → 值显示成 `—`。
     现在自动补的字段用固定 id `legacy-value`，跨次读取稳定不变。
  2. 漏网字段 id 回填到第一个字段时，回填前后数组**长度相同**（1 → 1），
     原来的「长度变了才写回」判断因此从不触发，修复等于没生效。
     改成**比较内容**（`fieldId` 和 `value` 是否都一致）后才写回。
  - 另外新增 `normalizeRecordValues()`，即使还没跑迁移，老式的 `{value: n}`
    记录也能直接读出来；迁移本身 `migrateLegacy()` 依然是**幂等**的，
    在每次启动、任何页面渲染之前执行一次。
- **`tools/gen-icons.py` 读不回旧图标**：它用的一直是「`name: '<path>'`」这种
  简单写法的正则，而 `icons.js` 实际是 `name: { cat, kw, path }` 对象格式，
  所以 `PATHS` 从没被旧文件填充过。改成按对象格式解析，并补了**自查**
  （重名、常用指向不存在的图标、分类之间重叠都会直接报错中止）。

## [v0.1.12] - 2026-09-22

### 新增

- **图标按类别分组 + 搜索**。图标从 62 个扩到 **85 个**，分到 9 个类别里
  （常用 / 运动 / 饮食 / 医疗健康 / 出行交通 / 学习工作 / 生活日常 / 娱乐休闲 / 其他）。
  - 「常用」是虚拟分类，成员从各分类里挑，所以会与归属分类重叠；
    **其他分类之间不重叠**。分类可折叠，只有「常用」默认展开。
  - 面板顶部加了**搜索框，中英双语**（如「药丸」「pill」「体重」「weight」都能搜到），
    输入即过滤，匹配到的图标平铺显示；搜不到给一句提示。
  - 新增的图标里有你要的**药丸**（`medication` / `medication_liquid` / `vaccines`）和
    **身材相关**（`accessibility` 人形 / `monitor_weight` 体重秤 / `straighten` 卷尺）。
    ⚠️ 谷歌经典 Material Icons **没有** `pill`，药丸只能用药瓶 / 药水 / 注射器表达。
- **跟踪统计改成「一次看全部项目」的折线图**。多项目跟踪项（如血压 = 高压 / 低压 / 脉搏）
  不再需要选「看哪个值」，而是每个项目画一条线，**靠线型区分**（实线 / 虚线 / 点线…），
  面板里给了对应图例。统计信息（次数 / 最新 / 平均 / 最大 / 最小）仍只针对主项目，
  并在第一行标明算的是哪个项目。
- 详情页的记录视图增加了**与时间页一致的视图栏**（左名称、右图标按钮，可切
  全部 / 最近一年 / 最近一月 / 最近一周），并按天插日期分隔线。范围是临时的，
  每次打开详情页回到「全部」。

### 变更

- 跟踪项表单里的「Fields / 字段」改成「**Items / 项目**」，标题的字号字重颜色与上面的
  「名称」统一（之前是小一号的栏目样式）。
- 时间记录表单的描述提示改成「Optional / 选填」，去掉了示例长句。

### 修复

- **统计图不居中**：绘图区左右内边距不对称（左 40 / 右 8），图整体向右偏了约 19px。
  改成左右各 38（左边要放纵轴刻度），绘图区现在严格居中。
- 纵轴刻度在小数（如 `130.48`）时前面几位被裁掉 —— 内边距太窄，已加宽。
- 图标面板折叠的分类**照样显示**：`display: grid` 盖掉了 `hidden` 的 `display: none`，
  补了 `.icon-grid[hidden]` 规则。
- 详情页菜单里的「删除」改成**红色**（`--danger`），与前面的重命名区分开。

## [v0.1.11] - 2026-09-22

### 变更

- **图标选择改成真正居中的模态弹窗**。之前它只是个贴着表单右沿弹出的小浮层，
  空间小、还容易和表单宽度打架；现在盖在整个视窗正中央，背后压一层遮罩，选完直接消失。
  宽度只受「视窗 − 两侧留白」限制，所以居中、不出屏。

### 修复

- 图标面板**垂直方向没真正居中**（`top: 50%` 和 `margin: auto` 打架，被顶偏了）。
  改用 `top/left 50%` + `translate(-50%, -50%)` 居中。
- 遮罩层级压过弹窗表单（`.sheet` 是 11，遮罩取 21、面板 22），
  不会再出现「表单浮在遮罩之上」的错层。

## [v0.1.10] - 2026-09-22

### 修复

- **图标选择面板横向溢出**（v0.1.8 引入）。面板宽度按 8 列算是 368px，但它是贴在表单行右沿、
  可用宽度只有「视口 − 触发按钮右沿」≈ 359px，于是最右一列被裁到屏幕外，
  还会把整个页面顶宽（可以横向拖动）。

### 变更

- **面板改成真正意义上的浮层**：不再挂在表单行里，而是挂到弹窗上。
  面板只需对齐触发按钮的右沿，宽度可以超出表单（表单只占视窗一部分），
  所以按「视口 − 两侧内边距」铺开；打开时按当前视口重算，宽度过宽就压回可用宽度、
  左侧放不下则整个挪到左内边距起算。
- 面板列数恢复 **8 列**，栅格改用 `minmax(0, …)`，窄屏上面板被压窄时格子等比例缩小，
  不会再出现「列数固定、总宽撑爆」导致的裁切。

## [v0.1.9] - 2026-09-22

### 变更

- **跟踪项表单的字段区去掉说明文字**。「一个值一行，例如：高压 / 低压 / 脉搏」那句话单独占一行、
  把字段输入框往下挤，现在直接把示例放进输入框当占位符
  （英文 `e.g. systolic` / 中文 `例如：高压`，原来是重复标签的「字段名」）。

## [v0.1.8] - 2026-09-22

### 修复

- **表单左沿参差不齐**。三个表单各自的毛病归到一起看，都是「这一行的东西没和上面的对齐」：
  1. **跟踪项表单的字段输入框**被当成「值」推到最右边，左沿跟上面的 `Name` 输入框对不上；
     现在字段区直接占满整行，输入框左对齐，和 `Name` 同一个起点。
  2. **行为 / 跟踪项表单的「图标」行**里，图标只有 24px 宽还被推到右边，视觉上跟 `Name`
     的值不在一条线上；改成 40×40 的方形热区，并去掉那个多余的下拉箭头
     （这行右侧本身就是图标，箭头纯属噪声）。
  3. **跟踪记录表单的多字段输入**原来是「一行一个字段名 + 右对齐输入框」，而
     `Systolic` / `Diastolic` / `Pulse` 名字长短不一，几行标签左沿就参差了；
     改成上下排（字段名一行、输入框一行，输入框左对齐），所有输入框都和时间 / 名称对齐。

### 变更

- **图标面板重排**：之前 `auto-fill` 的自适应列数在 412px 宽的手机上只排到 3 列，
  面板又窄又空；现在列数固定 8 列（`--icon-cols`），面板宽度按列数算好，
  末尾格子与触发按钮的右沿严格对齐，最后一行靠左对齐（不再居中）。
  格子大小 / 间距 / 内边距抽成 CSS 变量，JS 读这些值算宽度，以后改列数只动一个地方。

## [v0.1.7] - 2026-09-22

### 新增

- **图标选择改成「点开弹窗」**（图标已经 62 个，横向滑条太长了）。表单里只显示当前选中的
  一个图标 + 展开箭头，点一下弹出网格面板（`auto-fill` 自适应列数、超出可滚），选完自动收起；
  面板标题与关闭按钮的文案会跟着语言切换。

- **跟踪项可以是「多字段」的键值记录了**。建跟踪项时除了名称 / 图标，还能加任意多个字段，
  比如血压 = 高压 / 低压 / 脉搏（字段可随时增删改名，已有记录会按字段名对齐，不会丢数据）。
  记录表单为每个字段生成一个输入框，**至少填一个**就能保存；卡片左边按「字段名 值」逐行显示，
  没填的字段显示 —。统计视图多出一行「展示哪个值」，可以切到任意字段看走势与统计。

### 变更

- **`metrics.csv` 改成「一条记录一行」的宽表**：表头是 `id,metric,time,主值列名,其他字段列名…`，
  所以像 Excel 那样手工加一列（比如再加个「体温」），导回来就会自动变成新字段；
  旧格式（`id,metric,time,value`）照样能读，会当成「只有一个叫 value 的字段」。
  字段名里不能当列名的字符（逗号、引号、换行）会被去掉，重名自动加序号。

- **导出文件名自动编号**：从时间戳改成 `livolog-001.csv`、`livolog-002.csv`…（序号只在写成功后前进），
  这样多次导出不会互相覆盖。

### 修复

- **启动时只从磁盘载入一次 CSV**。之前每次从后台回到前台都会再读一次并覆盖网页里的数据，
  极端情况下会把正在编辑 / 刚新增的记录抹掉；现在只有「本次进程还没载入过」时才读。

## [v0.1.6] - 2026-09-22

### 修复

- **后台久置回来自屏**（必须解决的问题）。原因有两个，都跟 Android 回收资源有关：
  1. 系统回收了 **WebView 的渲染进程** —— 旧 WebView 只剩一层壳，页面永远不会再出来；
  2. Activity 被重建时 WebView 试图恢复「旧状态」，但渲染进程已经没了，恢复出来就是空白。

  现在做了三层自愈：重写 `onRenderProcessGone`，遇到渲染进程退出就**整只换掉 WebView** 并重新加载
  （返回 true，不让系统连 app 一起杀掉）；Activity 重建时不再让 WebView 恢复旧状态，
  而是老老实实重新加载入口页；每次回前台再做一次**探活**（问网页 `window.Livolog`，
  答不上来 / 超时就重新加载，带节流，健康时不会多加载一次）。

### 新增

- **时间记录可以写描述**（选填）。表单在时间下面多了一栏「描述」，
  卡片上会在主文字下方用浅色小字显示一行；**没写描述的记录不会出现这一行**，卡片高度和以前一样。
  行为详情页里的记录列表同样会显示。描述会随记录一起写进 `records.csv`
  （新增 `note` 列，按 RFC4180 转义，含逗号 / 引号 / 换行都不会错；旧文件少这一列照样能读）。

## [v0.1.5] - 2026-09-21

### 新增

- **统计图可以点某一天看具体数值**。图里每一天都有一整列透明命中区，点一下就在那根柱 / 那个点
  旁边浮出一个气泡：行为统计显示可读的时长（`8h` / `20m`）、跟踪统计显示当天取值（`72`）；
  折线图还会多画一条竖向指示线；再点同一天收起，点没有记录的天不弹。
  气泡画在 SVG 里（viewBox 坐标），所以跟着图一起缩放。

### 变更

- **时间页的天标识改成 `YYYY-MM-DD, 周几`**（如 `2026-09-19, Sat`，中文为 `2026-09-19, 周六`），
  比之前的 `09-19` 更好认。

- **新建时间记录时会按行为的习惯预选记录类型**。选完行为（或打开表单时默认的第一个行为）后，
  自动填上这个行为「偏向」的类型：记过的次数多的那种，一样多就用最近一次；
  从没记过就不猜（仍显示「选择类型」）。自动填完照样可以手动改，不会锁住。

## [v0.1.4] - 2026-09-21

### 新增

- **行为页 / 跟踪页的卡片可以长按拖动排序**。长按约 0.35s 进入拖动（卡片抬起、带着阴影），
  拖动时就地跟相邻卡片换位，松手写回数据层；已经在滚列表时不会误触发长按。
  拖完紧跟的那次点击会被吞掉，不会顺手点进详情页。
  行为页的顺序同时决定**时间页新建记录时「行为」下拉的顺序** —— 下拉里第一项就是列表第一项。

### 变更

- **统计视图的排版**：图下方的统计信息左右各留 20px，和上方选项栏里的文字对齐
  （之前贴到了卡片边缘，比选项文字多出 20px）。
- **「Range」那一行重排**：左边「Range」垂直居中，右边开始时间在上、结束时间在下、都贴右，
  两端仍各自可点（点哪端改哪端）。之前是标题占一行、时间在第二行分列两边。

## [v0.1.3] - 2026-09-21

### 新增

- **时间页的记录按天分隔**。所有视图（全部 / 最近一年 / 最近一月 / 最近一周）里，
  同一天只出现一条日期标记（如 `09-15`），换了天才插一条 —— 一周里只记了两天也能一眼看出
  记录分别属于哪几天。标记比「年 / 月 / 周」分区标题更轻（12px / 700 / 浅色），只做分隔。

### 变更

- **启动页改成「品牌黑」**。以前底色跟随日间 / 夜间主题，白天主题下会在白背景里浮着一块黑色图标；
  现在整屏固定品牌黑（`#1B1B1B`，与桌面图标底色相同）、去掉图标底块、logo 放大 1.5 倍 ——
  要突出的是 **Livolog 本身**，不是一块图标；`LIVOLOG` 字样固定用亮色。

- **不再被系统自带的图标启动页抢戏**。Android 12+ 默认会在启动时把「应用图标」放大着显示一屏，
  现在用 `androidx.core:core-splashscreen` 接管：把那张图标换成全透明图，只留品牌黑底色，
  并且 API 26+ 行为一致（不再看厂商脸色）。启动动画期间原生保持**浅色系统栏图标**与黑色窗口底色，
  网页演完通过新增的 `LivologNative.finishSplash()` 通知原生切回正常主题。

- **启动动画时长 1.4s → 2.2s**（前 4/5 保持不透明，最后 400ms 淡出），
  结束时由 `animationend` 触发收尾，另有 3s 兜底（万一系统把动画全关了）。

### 修复

- **跨天时段在统计里按天拆分**。`23:00 → 次日 07:00` 这 8 小时以前整段算在开始那天，
  现在按实际跨过的时长分摊到每一天（同一条记录在图上会变成相邻两根柱：60 分钟 + 420 分钟）。
  新增 `LivologChart.bucketSpans()`；范围筛选也从「只看开始那天」改成「与区间有重叠」，
  跨天记录不再因为开始日不在区间里而整段丢掉。

## [v0.1.2] - 2026-09-21

### 新增

- **带 logo 的启动动画（splash）**。冷启动时先盖一层全屏遮罩：中间是**与 launcher 图标完全相同**的
  黑底圆角图标（圆环用 `conic-gradient` + `clip-path` 复刻 Android 的 sweep 渐变，几何与
  `ic_launcher_foreground.xml` 同一串数字），指针从 8 点方向逆着扫一圈回到设计位置（分针落在 12 点），
  下方浮现 `LIVOLOG` 字样，约 1.4s 后整页自己淡出。

  - 全过程由 CSS 动画完成（`forwards` 停在 `visibility: hidden`），**动画不依赖 JS**；
    `app.js` 只是在 1.5s 后把元素 `hidden` 掉，别留着挡无障碍树。
  - 背景与文字色走主题变量，日间 / 夜间都成立；`prefers-reduced-motion` 下不动指针、
    遮罩时长缩到 600ms。

## [v0.1.1] - 2026-09-21

### 变更

- **图标库加了 10 个新图标**（都是谷歌官方 Material Icons，行为 / 跟踪项共用同一个选择器，
  共 62 个）。除了用户点名要的**闪电** `bolt`、**显微镜** `biotech`、**药丸（药瓶）** `medication`，
  另外补了 `water_drop`（喝水）、`bedtime`（睡眠）、`sports_soccer`（球类）、
  `videogame_asset`（游戏）、`wash`（洗漱）、`local_florist`（园艺 / 植物）、`savings`（存钱）。

- **桌面图标留白略收一点**：圆环外径 **42 → 44**（视口 108），等比缩放
  （$s = 22/33 = 2/3$，这档所有数字都很整：线宽 4.67、两针宽 4.00、中心点半径 2.00）。
  现在到 66dp 安全圆留 11、到 72dp 圆形遮罩留 14。

### 修复

- **时间页新建记录后视图被重置**。在「最近一周 / 最近一月 / 最近一年」里新建一条记录，
  视图会立刻跳回「全部」。原因是提交后的那段「当前视图看不到这条记录就切过去」的逻辑
  还在拿记录类型（`period` / `moment`）和视图名比对，而视图早就改成时间范围了，
  比对必然不相等 → 落回默认值「全部」。现在改成只在新记录确实落在当前范围之外时
  （例如在「最近一周」里补一条上个月的）才切回「全部」。

## [v0.1.0] - 2026-09-21

### 变更

> 从这一版起进入 `0.1.x`：界面框架、数据模型与存储格式都稳定下来了，
> 剩下的都是在这个骨架上补细节。

- **统计视图重排**。原本「图类型 / 开始 / 结束」三行里，起止时间其实是同一件事的两端，
  现在合成**一行**：标题占第一行，开始时间与标题**左对齐**、结束时间与标题**右对齐**，
  时间另起一行，两端各自可点（点哪端就改哪端）。
  图下方的统计信息（条数 / 最新 / 平均 / 最大 / 最小）本来就是不可改的内容，
  不再套卡片与分隔线，直接按「左边深色粗体名称、右边浅色数值」排在页面背景上。

- **折线图跨过没有记录的天**。之前只连「相邻且都有记录」的天，隔一天没记数据线就断了，
  看着像数据缺失；现在按「有记录的点」连线，中间没记录的天直接跨过去（也不把空数据算成 0）。

- **跟踪记录支持长按多选删除**（行为详情页同样补上）。以前只有时间页能长按删除，
  跟踪详情页的记录只能一条条点开、又没有删除入口 —— 现在与时间页一致：
  长按进多选、点顶部操作栏的垃圾桶删除。为此把多选操作栏的层级提到全屏详情页之上，
  并在关闭详情页时把多选目标交还给当前标签页。

- **修复新建时间记录时下拉选完不回显**：自绘下拉控件选完后没刷新按钮上的文字，
  所以「行为」一直显示第一个行为、「类型」一直显示占位符（数据其实已经改了，只是显示不对）。

- **设置页「数据存储位置」改名为 `Location`**（中文仍是「数据存储位置」）。

- **列表卡片左侧的文字加粗**。卡片左边是行为名 / 记录值，右边是时间，两边之前只靠颜色区分；
  现在左边改成粗体（`.card-title` 15px / 700），和右边的浅色时间拉开层次。

- **图标四周再放宽**：圆环外径 **46 → 42**（视口 108），等比缩放
  （$s = 21/33 \approx 0.6364$）。现在到 66dp 安全圆留 12、到 72dp 圆形遮罩留 15。

## [v0.0.20] - 2026-09-21

### 变更

- **列表卡片加上边缘**。卡片本身没有底色（就靠一条分隔线成组），只有下边框时，
  列表头和它下面的第一张卡片分不开。现在改成**每张卡片都画上边缘**、去掉下边框，
  相邻卡片之间不会出现双线（实测间距 0，共用一条 1px 细线），
  列表顶部的分区标题（如 `2026-09-W1`）下面也就有了一条明确的界限。
  设置页 / 统计视图那种带底色的卡片（`.setting-list`）同样加了一条上边缘，保持一致。

- **滚动条改为全局隐藏**。上一版把它做成 6px 的细滑块，但自定义 `::-webkit-scrollbar` 会让
  滚动条从「浮动淡出」变成「常驻且占 6px 宽」的经典滚动条，观感并不好，所以直接不画：
  `* { scrollbar-width: none }` + `*::-webkit-scrollbar { display: none }`。
  滚动照旧，内容宽度不再被挤掉 6px（实测 `clientWidth` 已等于 `innerWidth`）。

## [v0.0.19] - 2026-09-21

### 变更

- **图标四周再放宽**：圆环外径 **50 → 46**（视口 108），同样是等比缩放
  （$s = 23/33 \approx 0.6970$，比例不变）。现在到 66dp 安全圆留 10、到 72dp 圆形遮罩留 13。

- **时间页分组标签的字体统一**。之前三级各行一套字体，看着很乱：

  | | 之前 | 现在 |
  | --- | --- | --- |
  | 字号 | 12 / 14 / 13 px | **统一 13px** |
  | 字重 | 600 / 600 / 400 | **统一 700（标签）/ 400（层级名）** |
  | 颜色 | 浅 / 深 / 浅 | **统一：标签深色、层级名浅色** |
  | 大小写 | 年级全大写 + 字距 0.8px | **一律正常大小写、无额外字距** |

  层级现在只靠左侧缩进（20 / 32 / 44px）体现，不再靠字号字重颜色区分。

- **表单排版重新整理**（全站统一，`styles.css` 顶部有规则注释）：

  | 元素 | 规则 |
  | --- | --- |
  | 表单标题 `.sheet-title` | 深色 + **全大写** + 粗体 + **居中** |
  | 页面标题 `.page-title` | 深色 + **全大写** + 粗体 |
  | 栏目名 `.form-label` / `.datetime-label` / `.setting-label` | 深色 + 粗体，保持正常大小写 |
  | 值 `.form-input` / `.form-select` / `.seg` / `.setting-value` | 浅色 + 正常字重，保持正常大小写 |
  | 底部按钮 `.btn`（取消 / 确定） | **全大写** |

  值改成浅色后和占位符容易混淆，所以占位符额外加 `opacity: 0.6` 区分。
  中文不受 `text-transform: uppercase` 影响，所以中文界面不会变小写。
  设置页与统计视图的行（`setting-label` / `setting-value`）一并按这套规则对齐。

- **滚动条也换成同一套黑白风格**：去掉轨道底色与角块，只留一条 6px 的圆头滑块，
  颜色用前景色低透明度（日间 `rgba(27,27,27,.22)`、夜间 `rgba(236,236,234,.24)`），
  按下/悬停时加深到 `.45` / `.48`。这样在页面、卡片、弹窗各种底色上都成立。
  用 `::-webkit-scrollbar` 实现（WebView 是 Chromium 内核）；
  副作用是滚动条从「浮动淡出」变成「常驻占位」，会占 6px 宽度。

## [v0.0.18] - 2026-09-21

### 变更

- **内置粗体从 SemiBold 换成 Bold**：SemiBold 在 15px 的黑白界面上几乎看不出「粗」，
  标题、按钮、选中态这些靠字重区分的地方失去层次。
  现在 400/500 走 Regular、600/700 走 Bold（实测粗体笔画覆盖面积比 Regular 多约 41%）。
  两个子集文件依旧各约 1.5 MB，`livolog-mono-semibold.woff2` 删除。
- **图标再缩一档**：圆环外径 **56 → 50**（视口 108）。上一版把外径收到 56 后，
  跟遮罩边缘的空隙还是偏小；按同样的等比缩放（$s = 25/33 \approx 0.7576$）收到 50 后，
  到 66dp 安全圆留 8、到 72dp 圆形遮罩留 11。造型比例没动。
- **时间页分组标签改成紧凑的日期格式**：

  | 层级 | 之前 | 现在 |
  | --- | --- | --- |
  | 年 | `2026` | `2026`（不变） |
  | 月 | `2026-9` | **`2026-09`**（月份补零，符合 `YYYY-MM`） |
  | 周 | `W3 · 09-15 ~ 09-21` | **`2026-09-W3`**（去掉冗余的日期区间） |

  中英文统一用这套数字格式；右侧的层级名（年/月/周、Year/Month/Week）不变。
  `weekRangeLabel()` 随之删除。

## [v0.0.17] - 2026-09-21

### 变更

- **全项目更名 LifeLog → Livolog**（LifeLog 重名太多）。涉及：
  - 应用显示名、网页标题、i18n 的 `app.name`；
  - **包名** `com.eliaszwc.lifelog` → `com.eliaszwc.livolog`（源码目录、`namespace`、`applicationId`）；
  - **仓库名** `EliasZWC/LifeLog` → `EliasZWC/Livolog`（应用内更新的 API 地址同步改）；
  - 存储目录 `Documents/LifeLog/` → `Documents/Livolog/`；
  - 网页里的全局名（`LivologUI` / `LivologStore` / `LivologNative` / `LivologShell` …）、
    `localStorage` 键前缀、偏好文件名、主题名（`Theme.Livolog`）、CI 产物名。

  > ⚠️ 因为包名变了，系统会当成另一个应用：**需要先卸载旧的 LifeLog 再装 Livolog**，
  > 否则桌面会同时出现两个图标。

- **内置更纱等宽黑体**（Sarasa Mono SC）作为界面字体：
  完整字体每个字重 24 MB，这里只保留「拉丁 + 常用标点 + 全角 + GB2312 全集 + 网页实际用到的字符」，
  转 WOFF2 后 Regular / SemiBold 各约 1.5 MB（`assets/www/fonts/livolog-mono-*.woff2`），
  APK 从 2.8 MB 涨到约 6 MB。界面上用到 400/500/600/700 四档字重，
  两个文件各吃两档（400–500 / 600–700），映射保持确定。
  没打进包里的生僻字自动回退到系统字体，不会出现方块。
  授权 SIL OFL 1.1，授权文件一并放在 `assets/www/fonts/`。

- **图标四周留出边距**：圆环外径从 66 收到 **56**（视口 108）。
  原因：自适应图标「圆形遮罩」的可见直径就是 66，原尺寸正好贴着遮罩边界。
  做法是把定稿整体等比缩小（$s = 28/33 \approx 0.8485$），比例一点没改，
  现在到 66dp 安全圆还有约 4.6 的余量、到 72dp 圆形遮罩约有 7.6。

### 迁移（旧版本数据不会丢）

包名、偏好文件名、`localStorage` 键前缀、存储目录同时变了，所以三处各做了一次性迁移：

| 旧 | 新 | 迁移方式 |
| --- | --- | --- |
| `SharedPreferences("lifelog")` | `SharedPreferences("livolog")` | `MainActivity.migrateLegacyPrefs()`，启动时把旧文件里的键值整体拷过来（主题、自选存储文件夹、更新状态） |
| `localStorage['lifelog.*']` | `localStorage['livolog.*']` | `index.html` 顶部最早执行的那段脚本里搬（在套用主题之前），旧键保留不删 |
| `Documents/LifeLog/` | `Documents/Livolog/` | `CsvStore.migrateFromLegacyDir()`：新目录读不到、旧目录有时，把内容写进新目录再返回 |

### 修复

- 设置页「导出数据」在浏览器预览时下载的文件名改为 `livolog.csv`（原 `lifelog.csv`）。

## [v0.0.16] - 2026-09-21

### 变更

- **更换应用图标**：由原来的「三条日志条 + 圆点」换成一枚**白 → 灰逆时针环绕渐变的时钟**。
  黑底保留（`#1B1B1B`），造型是圆环 + 中心指针 + 1 点方向的一个圆点。

  几何（108 × 108 视口，见 `res/drawable/ic_launcher_foreground.xml`）：

  | 元素 | 参数 |
  | --- | --- |
  | 圆环 | 外半径 33 / 线宽 7 / 中线 29.5，圆心 (54,54) |
  | 圆弧跨度 | 12 点起逆时针 300°，**尾部截止在 2 点方向**（缺口在 12 点～2 点之间） |
  | 两端圆头 | 半径 3.5：12 点纯白、2 点最深灰 `#6E6E6E` |
  | 渐变 | 12 点纯白 → 逆时针逐渐变深 → 2 点达到 `#6E6E6E` |
  | 1 点圆点 | 半径 3.5，与圆弧尾部同色 |
  | 分针 | 长 16，指向 12 点 |
  | 时针 | 长 11，指向 8 点方向（顺时针 240°） |
  | 两针 | 线宽 6（等粗），圆头 |
  | 中心圆点 | 半径 3 = 指针半宽，与两针相切（正好填平圆心的凹角且不外溢） |

  实现要点：圆环当作**填充型圆环扇形** + 两端独立的实色小圆（因为 Android 的
  VectorDrawable **不支持给描边上渐变**，只能给填充上渐变），渐变用 `<gradient type="sweep">`。
  Android 的 sweep 渐变**没有起点角度参数**，固定从 3 点方向起、顺时针走一圈，
  所以按「12 点 = offset 0.75、2 点 = offset 0.9167」映射（详见该文件顶部的注释）。

## [v0.0.15] - 2026-09-21

### 新增

- 设置页「通用」区增加**语言**选项（在主题之前）：默认英文，可切换为中文。
  词条早已备好，这次把入口接上了；选择会记在 `localStorage`，重启仍然生效。
- 设置页「关于」区增加**联系**，显示 `eliaschang@163.com`，点击用系统邮件应用发信
  （新增 `LifeLogNative.openExternal(url)` 桥接，原生用 `ACTION_VIEW` 打开）。

### 修复

- **折线图线条过粗、刻度文字发糊**：画完图后把 `chart-bar` / `chart-line` 这类类型 class
  也写到了 `<svg>` 根上，而 `stroke` / `stroke-width` 在 SVG 里是可继承的，
  于是描边渗到了所有子元素（连刻度文字都被描了边），看上去又粗又模糊。
  现在根上只留 `chart`，类型改放在 `data-chart-type`；另外给根与刻度显式加了 `stroke: none` 兜底。

### 变更

- 设置页分区「个性化」更名为「通用」。

## [v0.0.14] - 2026-09-21

### 变更

- 时间页的分区层级改为**跟着时间范围走**，已经固定了的层级不再重复显示：
  - 全部 → 年 / 月 / 周
  - 最近一年 → 月 / 周（不再显示年）
  - 最近一月 → 周（不再显示年、月）
  - 最近一周 → 不分分区，直接列记录
- 分组右侧的提示由「条数」改为**层级名**（年 / 月 / 周），层级一眼能看出来。

## [v0.0.13] - 2026-09-21

### 新增

- **时间页记录改为三级分组**：年 → 月 → 周，每一级都可展开 / 折叠，标题右侧带条数。
  周按「月内第几周」算（1–7 号为第 1 周，…），这样周总是完整地落在某个月里，
  不会出现一个周横跨两个月、挂在哪边都不对的情况。折叠是就地切换，不重绘列表，滚动位置不跳。
- **统计视图增加选项栏**（行为详情页与跟踪详情页都有）：
  - 图类型：直方图 / 折线图
  - 统计区间：开始日期、结束日期（用新增的日期选择弹窗）
  - 横坐标刻度始终是「日」
  - 折线图的纵轴按数据的最小/最大取值铺开（否则体重这种集中在 70 附近的曲线会被压成一条直线）；
    没有记录的那几天是断开的，不会把「没数据」画成 0

### 变更

- **时间页视图栏的选项换成时间范围**：全部 / 最近一年 / 最近一月 / 最近一周（原来是全部 / 时段 / 时点）。
- **详情页的视图切换不再用下拉菜单**，改为标题栏正下方的一行内嵌导航栏（记录 / 统计），点哪项切哪项。
- 统计数字改按选定区间计算（以前固定按全部数据算）。
- 新增 `datepicker.js`（日期选择）与 `stats.js`（统计选项栏）；
  `chart.js` 重写为同时支持直方图与折线图，并改为按显式区间归集数据。

## [v0.0.12] - 2026-09-21

### 修复

- **修复「提示的是新版、装下去的却是旧版」的更新循环**（v0.0.10 → v0.0.11 时实测到）。
  根因：下载路径一直固定为 `cacheDir/update/lifelog-update.apk`，
  于是每次更新的 `content://` URI 完全相同，安装器（OPPO 的软件包安装程序尤其明显）
  会按 URI 复用上一次暂存过的那份包 —— 递过去的是 0.0.11，装下去的却是上次的 0.0.10。
  因为版本没变，下次进 app 又提示 0.0.11，形成无声循环。
  现在安装包放在 `cacheDir/update/<version>/lifelog.apk`，每次的 URI 都不同，
  下载前还会清空整个 `update/` 目录。

### 新增

- **拉起安装器前校验安装包**：用 `PackageManager.getPackageArchiveInfo()` 解析并逐项检查
  字节数与 Release 声明一致、`versionName` 与发布标签一致、
  `versionCode` 严格大于当前已安装版本。任何一项不过就直接报错，不会把错的包递进安装器。
  新增失败原因：`invalid` / `truncated` / `mismatch` / `downgrade`，均有对应中文提示。
- 记录「上次拉起安装器时装的是哪个版本」：下次进入前台若又检测到同一版本、
  而 app 自己的版本号没变，弹窗会额外提示一句「上一次安装没有生效」，
  而不是让同一个弹窗无声地循环出现。

### 变更

- 更新包下载禁用 HTTP 缓存（`useCaches=false` + `Cache-Control: no-cache`）。
- 点遮罩关闭更新弹窗时也会通知原生，避免原生侧状态卡在「更新进行中」
  而从此不再检查更新。

## [v0.0.11] - 2026-09-21

### 修复

- **设置页的名称真的左对齐了**。上一版只对齐了分区标题的内边距，没注意到「整行可点」的设置项是
  `<button>`，而浏览器给按钮的默认 `text-align` 是 `center`——名称文字其实是被居中的
  （实测在 155~223px 附近），只有非按钮的「版本」行是左对齐的。现在给这两处显式加了
  `text-align: left`，所有名称的文字左边缘与分区标题一致。

### 变更

- 设置页相关的命名由 `settings` 统一改为 `setting`：导航词条 `nav.setting`、
  `data-page` / `#page-setting` / `#tab-setting`、i18n 键前缀 `setting.*`、CSS 类 `.setting-*`、
  模块名 `LifeLogSettingPage`、文件名 `setting.js`，英文标题与导航文案改为「Setting」。

## [v0.0.10] - 2026-09-21

### 新增

- **跟踪页**（导航栏第三项，填充图标）：不记录日常活动，而是跟踪各种数据——体重、腰围、
  每天的喝水量等等。交互与行为页一致：列表 → 点进去是具体信息页（标题栏 + 视图栏）。
- 跟踪详情页的**记录视图不一样**：跟踪记录不属于时间记录，加不进时间页，
  所以这一页自带一个悬浮按钮作为唯一入口，表单里也只有「记录时间 + 记录值」，
  不用填起止时间（本质上就是时点）。统计视图同样是上面直方图（每日取值）加下面统计文本。
- 跟踪数据单独存成 `metrics.csv`（与 `records.csv` 同目录），格式 `id,metric,time,value`。
- 设置页的**数据存储位置可以换文件夹了**：点那一栏拉起系统文件夹选择器，
  授权会持久保留；如果目标文件夹里已经有 `records.csv` 就采用它，没有就把当前数据搬过去。
  长按那一栏可以恢复默认位置（`Documents/LifeLog`）。

### 变更

- 时间记录卡片的日期**只显示开始那天的日期**，跨天的时段也不再展开成两段日期。
  只是显示变简单，存储的数据一个字节都没改。
- 设置页的分区标题改为与设置项名称左对齐（此前差 4px）。
- 时间页的分段日期时间输入与直方图抽成了 `datetime.js` / `chart.js`，与跟踪页共用。
- 行为图标库增加到 52 个（新增健身 / 体重）。

## [v0.0.9] - 2026-09-21

### 新增

- **应用内检测更新**：每次重新进入 app 都会查一次 GitHub 最新 Release，
  有新版本就弹窗告知（版本号 + 包大小 + 当前版本）。确认后由 app 自己下载，
  下载完直接拉起系统安装器；若系统未授予「安装未知应用」权限，会先把用户送去授权页，
  回来后可以点「重试安装」，不必重下。
- 时间记录卡片现在**点击即可修改**：弹出与「新增记录」相同的表单，行为 / 类型 / 起止时间都能改，
  提交后原地更新而不是新增一条。行为详情页里的记录列表同样支持。
- 设置页「数据管理」拆成三栏：数据存储位置 / 导入数据 / 导出数据；
  「导出数据」拉起系统「另存为」，把 CSV 存到用户自己选的位置。

### 变更

- 设置页所有值右对齐，且宽度不超过整行的一半，超长内容以省略号截断。
- 行为详情页删除确认弹窗的「删除」按钮文字改为红色（全站唯一打破黑白的地方，仅用于破坏性操作）。
- 切换记录类型（时段 / 时点）时保留已填写的时间，不再重置为「现在」。

## [v0.0.8] - 2026-09-21

### 新增

- **行为详情页的统计视图**：上方一个直方图（纵坐标 = 时长累计（分钟），横坐标 = 天数），
  下方统计信息文本（总时长、总次数、平均每次时长、平均每日时长）。
  直方图用内联 SVG 手绘，不引第三方图表库；时间窗口随数据自适应，最少 7 天、最多 30 天。
  只有「时点」记录、没有时长可言的行为，直方图退化为按天记次数。
- 行为可选的图标由 31 个增加到 51 个，补充了用药、吸烟、通勤、地铁、打车、轮渡、篮球、
  快餐、披萨、洗衣、冥想、思考、影音、记账、翻译、文档、通话、邮件、购物袋、收藏等常用图标。

### 变更

- **设置项统一为「左名称 / 右当前值」**：点整行才弹出选项，不再在行内嵌下拉控件；
  版本号与数据库文件位置作为只读项，同样遵循该布局。
- 设置列表改为**卡片式栏**（背景色取 `--surface`，与页面背景 `--bg` 拉开差异），
  上下列之间有分隔线，一眼能看出「这是一栏」。
- 导航栏「时间」图标由 `schedule`（线框钟表）换成 `hourglass_full`（实心沙漏），
  与「行为」「设置」都统一为填充式；「行为」图标由 `touch_app` 换成 `category`（分类）。

## [v0.0.7] - 2026-09-21

### 新增

- **行为详情页**：点击行为卡片进入。标题栏居中为行为名称，左侧返回、右侧菜单（重命名 / 删除）；
  标题栏下方是视图栏（记录 / 统计），记录视图列出该行为下的全部时间记录（布局同时间页），统计视图暂时留空。
- 删除行为需**输入行为名称**确认（类似 GitHub 删仓库），删除时连带清掉该行为名下的全部时间记录。
- 行为卡片右侧增加箭头图标，提示点进去还有内容。
- **CSV 数据库**：全部时间记录以 CSV 形式落盘到手机的 `Documents/LifeLog/records.csv`；
  启动时从该文件载入，之后任何改动都会同步写回。
- 设置页「数据管理」新增「导入 CSV」，可从文件管理器选文件导入数据库。
- 设置页改为分区布局：个性化（主题）/ 数据管理（导入 CSV + 数据库文件位置）/ 关于（版本）。

### 变更

- 导航栏「行为」图标由 `task_alt`（待办语义）换成 `touch_app`（行动语义）。
- 行为列表的长按多选删除作废，删除入口改到详情页菜单（时间页的长按多选保留）。

## [v0.0.6] - 2026-09-21

### 修复

- 表单弹窗打开时，状态栏区域不再保持页面亮色，遮罩现在铺满整屏。
- 键盘弹出时表单不再被输入法盖住：原生把输入法高度传给网页，表单整体上移。
- APK 改用固定发布密钥签名。此前缺少密钥时会回退到 debug 签名，而 CI 每次构建都会新生成随机密钥，
  导致新旧版本签名不一致、覆盖安装报 `conflicting signature`。现在缺少密钥时 release 构建直接失败。
- 设置页的版本号只显示版本名，形如 `0.0.6`。
- 修复启动闪退（根因）：`onPageFinished` 里的 `findViewById(R.id.root)` 位于 `configureWebView()` 的
  `with(webView) { ... }` 作用域内，被解析成 `webView.findViewById(...)`；而根视图是 WebView 的**父容器**，
  从 WebView 往下找只会得到 null，于是 `requestApplyInsets(null)` 抛 NPE。改为把根视图存成字段引用。
- 以下为排查过程中顺手做掉的加固：
  - JS 桥 `WebAppBridge` 改为独立的 **public 类**（原先是 `private inner class`，而 Android 的 JS 桥
    是用反射查找并调用 `@JavascriptInterface` 方法的，非 public 类在部分系统上会调用失败）。
  - `evaluateJavascript` 改为**页面加载完成后**才注入，不再在首次布局时就调用。
  - `onPageFinished` 的 `url` 参数按可能为 null 处理（原先声明为非空，收到 null 会抛异常）。
  - 窗口背景 / 状态栏图标相关操作全部用 try/catch 包住，纯外观问题不再导致崩溃。
- 新增崩溃诊断：未捕获异常会写入应用私有目录，下次启动直接把堆栈显示在屏幕上，没有 adb 也能定位。

### 新增

- 卡片支持长按进入多选，可批量删除；顶栏显示已选数量并提供删除与取消入口。
- 设置页第一项显示版本名与版本号（只读），数据由原生从 `build.gradle.kts` 读取后推送。

### 变更

- 所有下拉选项改为自绘控件（原生 `<select>` 在 WebView 里只能弹系统样式），下拉菜单与 app 风格一致。
- 时段的时间连接符由横杠 / 箭头统一改为波浪号 `~`。
- 移除底部导航栏顶部的横线。
- 调整配色：日间页面背景比导航栏更深，夜间页面背景比导航栏更浅（导航栏始终处在更“极端”的一端）。
- WebView 改为真正全屏，内容改由网页根据原生推送的系统栏尺寸自行让位。

## [v0.0.5] - 2026-09-21

首个发布的版本。

### 变更

- 表单弹窗的操作按钮改为纯文字样式：Cancel 左对齐、Confirm 右对齐。
- Confirm 去掉药丸背景，改为高对比文字（日间近黑 `#1B1B1B`、夜间近白 `#F2F2F0`）；Cancel 为灰色。
- 两个按钮都加入按下动画（缩小 + 淡出）；不可用时 Confirm 退为灰色。

## [v0.0.4] - 2026-09-21

### 新增

- 底部导航调整为三项：时间 / 行为 / 设置。
- 时间页新增视图栏：左侧为当前视图名称，右侧为图标按钮，点击下拉切换「全部 / 时段 / 时点」三个视图。
- 时间页、行为页改为列表布局，右下角增加悬浮圆形按钮（仅加号图标），点击打开各自的表单弹窗。
- 行为页表单：名称 / 图标 / 取消·确定 三行。图标从谷歌官方图标库中选（内置 31 个），确定后生成卡片（图标在左、名称在后）。
- 时间页表单：行为 / 类型 / 时间 / 取消·确定 四行。
  - 行为是时间记录的前提，需先在行为页建立行为。
  - 类型决定时间填写方式：时段填开始与结束，时点只填发生时间；未选类型前时间行不可填写。
  - 时间按 `YYYY-MM-DD-HH:mm`（24 小时制）分段填写，每位固定、仅接受数字，填满自动跳到下一位。
  - 卡片：行为图标左对齐、名称随后；时间右对齐并分两行（年月日在上、时分在下）。
- 页面与列表切换时增加方向性滑入动画（向右切从右进，向左切从左进），并遵循 `prefers-reduced-motion`。
- 新增数据层 `store.js`（行为与时间记录，落在 localStorage）。

### 变更

- 导航图标改为 `schedule` / `task_alt` / `settings`，悬浮按钮与下拉箭头等均使用谷歌官方图标。

### 移除

- 移除原「统计」页入口（时段 / 时点改为时间页内的视图）。

## [v0.0.3] - 2026-09-21

### 新增

- 每个页面顶部增加居中标题，标题文案随当前页与语言切换。
- 设置页：改为「列表 + 尾部下拉框」布局，新增第一项设置「主题」。
  - 三个选项：日间模式 / 夜间模式 / 跟随系统模式。
  - 手动选择会覆盖系统配色（网页端 `<html data-theme>`），原生端同步窗口背景与状态栏图标颜色。
- 新增 `assets/www/theme.js`（主题偏好）与 `assets/www/settings.js`（设置页绑定）。
- 原生新增 `LifeLogNative` JS 桥接，供网页向原生同步主题状态。

### 变更

- 布局与窗口背景改由 `MainActivity` 统一按应用内主题上色，避免系统与应用内主题不一致时露出错色。

## [v0.0.2] - 2026-09-21

### 新增

- 底部导航栏扩展为四项，从左到右为：时段 / 时点 / 统计 / 设置；四个页面均暂时留空。
  - 时段：记录有持续过程的行动（如睡眠从几点到几点）
  - 时点：记录一过性的行动（如吃药）
  - 统计：汇总展示记录数据
  - 设置：修改应用设置
- 新增极简 i18n（`assets/www/i18n.js`），默认语言为英文，中文词条已备好。

### 变更

- 导航栏选中态去掉胶囊/外框背景，改为仅用颜色与字重区分，与黑白极简风格一致。
- 导航图标更换为 `date_range` / `schedule` / `bar_chart` / `settings`（均为谷歌官方 Material Icons）。

## [v0.0.1] - 2026-09-21

### 新增

- 初始化 LifeLog 项目骨架（Android WebView 套壳 + 网页前端）。
- 底部导航栏：两个入口「日志」「统计」，均为图标在上、文字在下的样式。
- 「日志」「统计」两个页面暂时留空。
- 黑白主题：白天以白为主、夜间以黑为主，均使用非纯黑纯白的质感灰阶。
- 图标全部采用谷歌官方 Material Icons。
- GitHub Actions：推送构建 Debug APK；打 `v*` 标签自动构建并发布 Release。
